# Incremental Offline Map Update Workflow

این بسته workflow انتشار نقشه را طوری تنظیم می‌کند که نسخهٔ جدید هر کشور با نسخهٔ قبلی مقایسه شود و patch تکه‌ای منتشر شود. اپلیکیشن، در صورت تطابق SHA-256 نسخهٔ نصب‌شده، فقط patch را دانلود و روی فایل موجود اعمال می‌کند؛ در غیر این صورت دانلود کامل به‌عنوان fallback انجام می‌شود.

## تغییرات

- `.github/workflows/Build-and-publish-ABM-v4-fixed.yml` اضافه شد.
- workflow نسخهٔ قبلی همان کشور را از release `maps-v4` می‌گیرد.
- ABM جدید را می‌سازد و قبل از انتشار validate می‌کند.
- با `packaging/create_patch.py` فقط بلوک‌های تغییرکردهٔ ۴ مگابایتی را تولید می‌کند.
- اگر patch از فایل کامل بزرگ‌تر باشد، patch حذف می‌شود و manifest فقط دانلود کامل را معرفی می‌کند.
- با `packaging/build_release_manifest.py` مانیفست نهایی ساخته می‌شود.
- انتشار با `gh release upload --clobber` انجام می‌شود.
- ZIP ABM با timestamp ثابت ساخته می‌شود تا بازسازی‌های بعدی بی‌دلیل همهٔ chunkها را تغییر ندهند.
- graph خالی قبل از انتشار با خطای صریح متوقف می‌شود.

## اجرا

پس از قرار دادن محتویات این بسته در ریشهٔ repository، workflow در GitHub Actions فعال می‌شود. برای اجرای دستی می‌توان `country=IR` یا `country=all` انتخاب کرد.

برای اولین انتشار، چون نسخهٔ قبلی وجود ندارد، فایل کامل منتشر می‌شود. از انتشار دوم به بعد، دستگاه‌های دارای نسخهٔ قبلی patch را دریافت می‌کنند.

## تست انجام‌شده

ساخت نمونهٔ ABM و `verify_abm` موفق شد. ساخت دوبارهٔ یک ورودی یکسان، ZIP یکسان تولید کرد و patch آن صفر بایت بود؛ این نشان می‌دهد timestampهای ZIP دیگر باعث تغییر مصنوعی chunkها نمی‌شوند.
