# Localized UI assets

These JSON files are generated from `lib/core/localization/app_localizations.dart`.

- Current source key count: **585**
- 30 locale files are included.
- `generate_locales.py` keeps every locale on the same key set.
- Existing translations are preserved. New keys are seeded from English and should be translated before publishing a language pack.

Run from the project root:

```bash
python assets/local/generate_locales.py
```
